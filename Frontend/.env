# ── StudyFlow Frontend — Variáveis de ambiente ─────────────────────────────
# Copia este ficheiro para .env.local e preenche os valores.
# Ficheiros .env.local nunca são commitados ao Git (já no .gitignore do Vite).

# URL do backend FastAPI (sem trailing slash)
VITE_API_URL=http://localhost:8000
